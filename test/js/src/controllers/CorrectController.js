angular.module ("app").controller ('CorrectController',["$scope", "$log", function($scope, $log){
    $scope.data = ["data1", "data2"];
    
    $scope.onItemClicked = function () {
        // do something here
    };
}]);
